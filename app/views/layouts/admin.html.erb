<!DOCTYPE html>
<html>
  <head>
    <title>Адмінка</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_importmap_tags %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <%= stylesheet_link_tag "custom", media: "all" %>
  </head>
  <body>
    <div class="d-flex">
      <!-- Sidebar -->
      <nav class="bg-dark text-white d-flex flex-column p-3" style="width: 250px; min-height: 100vh; position: sticky; top: 0;">
        <h4 class="text-center mb-4">Admin Panel</h4>
        <ul class="nav flex-column flex-grow-1">
          <li class="nav-item mb-2">
            <%= link_to "📦 Замовлення", admin_orders_path, class: "nav-link text-white" %>
          </li>
          <li class="nav-item mb-2">
            <%= link_to "📚 Книги", admin_books_path, class: "nav-link text-white" %>
          </li>
          <li class="nav-item mb-2">
            <%= link_to "👤 Користувачі", admin_users_path, class: "nav-link text-white" %>
          </li>
          <li class="nav-item mb-2">
            <%= link_to "💬 Коментарі", admin_comments_path, class: "nav-link text-white" %>
          </li>
          <li class="nav-item mb-2">
            <%= link_to "📊 Аналітика", admin_dashboard_path, class: "nav-link text-white" %>
          </li>
        </ul>
        <div class="mt-auto">
          <%= link_to "⬅ На сайт", root_path, class: "btn btn-sm btn-outline-light w-100" %>
        </div>
      </nav>

      <!-- Main content -->
      <main class="flex-grow-1 p-4 bg-light" style="min-height: 100vh;">
        <%# Заголовок сторінки, якщо заданий %>
        <% if content_for?(:page_title) %>
          <h1 class="h3 mb-4"><%= yield :page_title %></h1>
        <% end %>
        <%# Breadcrumbs, якщо задані %>
        <% if content_for?(:breadcrumbs) %>
          <nav aria-label="breadcrumb">
            <ol class="breadcrumb mb-4">
              <%= yield :breadcrumbs %>
            </ol>
          </nav>
        <% end %>
        <%= yield %>
      </main>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
