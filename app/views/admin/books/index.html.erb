<div class="d-flex justify-content-between align-items-center mb-4">
  <h1 class="h3 mb-0">Список книг</h1>
  <%= link_to "Додати нову книгу", new_admin_book_path, class: "btn btn-success" %>
</div>

<div class="table-responsive">
  <table class="table table-striped table-hover align-middle bg-white border rounded shadow-sm">
    <thead class="table-dark">
      <tr>
  <th>Назва</th>
  <th>Автор</th>
  <th>Категорія</th>
  <th>Знижка</th>
  <th>Ціна</th>
        <th class="text-center">Дії</th>
      </tr>
    </thead>
    <tbody>
      <% @books.each do |book| %>
        <tr>
          <td><%= book.title %></td>
          <td><%= book.author %></td>
          <td>
            <% if book.category.present? %>
              <span class="badge bg-gradient-category"><%= book.category %></span>
            <% else %>
              <span class="text-muted">—</span>
            <% end %>
          </td>
          <td>
            <% if book.discount.to_f > 0 %>
              <span class="badge bg-gradient-discount">-<%= book.discount.to_i %>%</span>
            <% else %>
              <span class="text-muted">—</span>
            <% end %>
          </td>
          <td><%= number_to_currency(book.price, unit: "$") %></td>
          <td class="text-center">
            <%= link_to "👁 Деталі", admin_book_path(book), class: "btn btn-sm btn-outline-secondary me-2" %>
            <%= link_to "Редагувати", edit_admin_book_path(book), class: "btn btn-sm btn-outline-primary me-2" %>
            <%= link_to "Видалити", admin_book_path(book), method: :delete, data: { confirm: "Ви впевнені?" }, class: "btn btn-sm btn-outline-danger" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
